# LLM Reliability Lab

This is an original synthetic Python demonstration with a fully local runtime. Read SPEC.md
before edits. Never read private source material or call live model APIs.
Only the orchestrator edits shared contracts, dependencies, or integrates work.
Concurrent writers use distinct Git worktrees and remain within assigned paths.
Do not revert another worker's work. Record limitations, real commands and results.
Use configured project identity for both author and committer; inspect environment
overrides before every commit. Preserve actual timestamps for new work and the
existing signing configuration. The one-time user-authorized date reconstruction
on 2026-09-12 is documented in `docs/history-timeline.md`; it does not authorize
inventing dates for subsequent work. No optional attribution trailers. On 2026-09-12, the user authorized
publishing this repository publicly at `DerekW00/llm-reliability-lab` on GitHub,
including the necessary publication documentation and main-branch pushes.
Future pushes need task authorization. Package releases, hosted deployments and
changes to other repositories are outside this publication authorization.
Project state belongs in STATUS.md. No external workspace records are needed.
