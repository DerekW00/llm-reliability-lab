# Generated synthetic fixture reports

These three JSON/Markdown pairs were generated by the evaluator source at
`5ce81ab5057a822c5c115bcff4d22f35a0d31c68`, from a clean checkout on 2026-09-12 UTC.
They were regenerated at that revision because a second independent review changed
how the report displays supplied identifiers and quoted document text; the pairs
previously here were generated at `9b1ec55b221e98c7a0ce46fa34b1623002e55a44`, and
that earlier source is still in the history. Counts, metrics, differences and gate
decisions are identical across both — only the display notation and the recorded
provenance moved. The JSON includes exact UTC times, local evaluation duration,
Python version, code revision, content fingerprints and replay inputs.

Supplied identifiers and quoted document lines appear in JSON notation, so a value
shown in a report parses back to exactly the value evaluated, and two documents that
differ only by a control character do not render alike. Field names and the split are
fixed by the contract rather than supplied, so they stay unquoted.

Commands run from the repository:

```sh
uv run reliability-lab demo --scenario baseline
uv run reliability-lab demo --scenario regression
uv run reliability-lab demo --scenario repaired
```

The commands wrote to ignored `reports/`; each generated pair was copied here
without editing its contents. Exits were 0, 1 and 0 respectively. These are
synthetic fixture scores, not inference or a benchmark of real LLM performance.

- [Baseline](baseline.md): 182 TP, 1 FP, 1 FN, 17 TN; 39/40 exact records;
  zero critical errors; accepted.
- [Regression](regression.md): 174 TP, 9 FP, 9 FN, 17 TN; 31/40 exact records;
  eight critical identifier errors; rejected.
- [Repaired](repaired.md): same counts as baseline; accepted.

Precision, recall and micro F1 happen to coincide for this constructed error mix.
Reproduction checks compare counts, metrics, differences and gate decisions;
timestamps, durations and source-context metadata can vary. File order does not
change content fingerprints. The dataset manifest separately pins raw file bytes.

Recompute the saved rejection (expected exit 1):

```sh
uv run reliability-lab compare \
  examples/reports/baseline.json examples/reports/regression.json \
  --output-dir reports/replayed
```

Do not treat embedded inputs or hashes as tamper-proof attestation. Comparison
validates their mutual consistency; it cannot authenticate who authored them.
